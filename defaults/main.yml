---
# The location of the mongod drop-in unit file that will be created.
mongo_dropin_unit_file: /etc/systemd/system/mongod.service.d/mongod.conf

# The version of MongoDB to install.  Valid values are 3.2, 3.4, 3.6,
# and 4.0.
mongo_version: 3.6
